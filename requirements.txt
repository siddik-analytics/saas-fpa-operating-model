# Phase 2 - Synthetic Data Foundation through Phase 9 - the Excel operating model.
# Lower bounds only: the generator uses stable APIs (numpy.random.Generator,
# pandas IO) and pinning exact builds breaks installation on newer interpreters.
numpy>=1.26
pandas>=2.1
PyYAML>=6.0
pytest>=8.0
duckdb>=1.1
# Phase 9 - the Excel FP&A operating model (openpyxl only; no VBA, no macros).
openpyxl>=3.1
